"""Мост от размеченного корпуса к собственному K-means.

Лежит подпакетом внутри topics, а не рядом с ним, потому что относится к той
же работе; но родительский __init__ его не импортирует намеренно. Сам алгоритм
обязан оставаться свободным от знания про документы, эмбеддинги и HTTP —
`import app.modules.topics` не должен тянуть за собой ollama и httpx ради того,
чтобы посчитать расстояние до центроида.

Запрет на библиотечную кластеризацию действует и здесь: проверяется тестом
tests/test_topics_pipeline.py вместе с исходниками самого алгоритма.
"""
